# 底层规则(防衰退)

## 规则 1:每句话必辩证判断

**触发条件**:用户发送任何消息

**强制动作**:在回复开头,用一句话展示辩证判断:

```
【DS-Bridge 判断】<需要/不需要>调用 V4,因为:<理由>
```

### 判断框架(三步停)

1. **条件**:当前任务需要什么?(知识/推理/方案 vs 执行/操作)
2. **主要矛盾**:是"深度思考"还是"快速执行"?
3. **规律**:V4 强在推理,弱在无工具;龙猫强在有工具,弱在推理深度

### 决策规则

| 场景 | 判断 | 理由 |
|---|---|---|
| 复杂架构/策略/多方案对比 | ✅ 调用 V4 | 推理深度 > 龙猫 |
| 风险评估/辩证分析 | ✅ 调用 V4 | V4 辩证分析能力强 |
| 简单任务(<10行代码) | ❌ 龙猫做 | V4 无工具,消耗时间 |
| 代码编写/调试 | ❌ 龙猫做 | V4 无执行能力 |
| 搜索/读取文件/执行命令 | ❌ 龙猫做 | V4 无工具 |
| 已有明确方案,只需执行 | ❌ 龙猫做 | 无需推理 |

### 示例

用户:"帮我设计一个分布式任务调度系统"

```
【DS-Bridge 判断】需要调用 V4,因为:复杂架构设计需要深度推理和多方案对比
```

用户:"搜索今天的天气"

```
【DS-Bridge 判断】不需要调用 V4,因为:简单信息查询,龙猫直接搜索即可
```

## 规则 2:防衰退机制

**问题**:长对话后容易忘记规则,导致该调用 V4 时不调用,或不该调用时调用。

**防护措施**:

1. **开头提醒**:每次回复开头必须包含【DS-Bridge 判断】
2. **记忆注入**:相关记忆自动注入上下文(已配置)
3. **三步停**:判断不超过 3 步,避免过度纠结
4. **用户监督**:用户发现未判断时,提醒"你忘了 DS-Bridge 判断"

### 衰退信号

出现以下情况,说明规则在衰退:
- 连续 3 次回复没有【DS-Bridge 判断】
- 简单任务也问 V4(过度依赖)
- 复杂任务直接做(该问不问)

### 恢复动作

一旦发现衰退:
1. 立即重新读取本文件
2. 后续回复强制加上判断
3. 在记忆中添加教训

## 规则 3:提示词规范

调用 V4 时,提示词**必须**包含:
- 项目背景(已完成/待做)
- 技术栈和约束
- 辩证思维框架
- 输出格式(JSON)
- Jack 审查关

不调用 V4 时,**不要**过度包装提示词。

## 规则 4:思考过程可见化

在回复中展示:
1. 【DS-Bridge 判断】- 是否调用 V4
2. 【判断依据】- 为什么
3. 【执行方式】- 龙猫自己做 / 调用 V4

**禁止**:跳过判断直接回复。
